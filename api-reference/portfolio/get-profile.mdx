---
title: "Get Your Profile"
openapi: "GET /profiles/{account}"
---

<Note>
  This endpoint returns your **internal user ID** (`id`) and **fee rate** (`rank.feeRateBps`), which are required when constructing signed orders via the API.
</Note>
