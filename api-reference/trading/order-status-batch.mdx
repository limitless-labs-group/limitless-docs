---
title: "Get Order Status (Batch)"
openapi: "POST /orders/status/batch"
---

<Info>
  Look up orders by either `orderId` (internal) or `clientOrderId` (your identifier). Provide exactly one per item — not both.
</Info>
