---
title: "User Login"
openapi: "POST /auth/login"
---

<Note>
  Cookie-based session auth is **deprecated**. For programmatic access, use an [API key](/developers/authentication) (`X-API-Key` header) instead.
</Note>
